# Normalize all text files to LF in the repo (cross-platform correct).
# Stops phantom CRLF<->LF "changes" that appear when editing on a Windows mount.
* text=auto eol=lf

# Binary assets — never touch line endings.
*.png binary
*.jpg binary
*.gif binary
*.webp binary
*.ico binary
