node_modules/
dist/
.env
__pycache__/
*.pyc
/data/

# Ship the built UI bundle so `kirocrew app install` works turnkey (manifest ui.entry
# = dist/index.mjs, resolved under ui/). The rest of dist/ stays ignored.
!ui/dist/
!ui/dist/index.mjs

docs/
